//accept a number of studdent from user: i need to give 5 applle to each student , how many apple i need.

#include<stdio.h>
int main()
{
    int student;

    printf("enterr the number of students:");
    scanf("%d",&student);

    printf("the number of apple are requiredd is %d",student*5);
    return 0;
}